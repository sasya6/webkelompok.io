// Anda bisa menambahkan script JavaScript interaktif di sini
// Contoh: Efek smooth scroll, validasi form, dll.

document.addEventListener('DOMContentLoaded', function() {
    // console.log("Website siap!");
    // Tambahkan script interaktif lainnya di sini
});